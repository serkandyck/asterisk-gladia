# Select the speech provider ('google' or 'gladia')
PROVIDER=google

# --- Gladia Configuration ---
# Required if PROVIDER=gladia
GLADIA_API_KEY=

# --- Google Configuration ---
# If using Google, ensure Application Default Credentials (ADC) are configured.
# See: https://cloud.google.com/docs/authentication/provide-credentials-adc
# Alternatively, set GOOGLE_APPLICATION_CREDENTIALS environment variable
# pointing to your service account key file.
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/keyfile.json
